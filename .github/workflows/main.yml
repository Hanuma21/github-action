name: check merging possibilities
on:
 
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Run a multi-line script
        run: echo hanuma starts to grow 😁 with the help of you ....
          echo you are on a platform not just about who gets credit its about who gets to build
          echo you are a good develepor so move forward🔥🔥🔥🔥
          
     
   
          
          
          
           
